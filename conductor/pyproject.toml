[project]
name = "dsh-novel-conductor"
version = "0.1.0"
description = "dsh-novel 无人值守流水线：按大纲连续写章 → 37 维评审 → 低于阈值回炉（OpenWrite HTTP 契约 + 可选 dsh SDK 指导综合）"
requires-python = ">=3.10"
dependencies = [
  "deepseek-harness-sdk>=0.1.1rc1",
]

[tool.uv]
# 复用仓库根的 uv 工作流；SDK 自带单文件 Node 运行时（macOS arm64 有 wheel）。
