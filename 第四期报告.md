# OpenWrite 项目第四期开发报告

> 报告日期: 2026-03-02
> 对比基准: 第三期审计报告
> 报告类型: 项目状态总结 + 文档更新

---

## 执行摘要

第四期主要聚焦于**项目文档完善与一致性维护**。经过前三期的重大架构升级（第一期基准审计、第二期 Director Tool Calling 调研、第三期模块化 Skill 架构重构），项目已进入稳定迭代阶段。

**本期重点**：
1. 全面梳理项目当前状态
2. 更新 AGENTS.md 和 README.md 至最新架构
3. 详细记录目录结构和开发规范，确保后续对话一致性
4. 标记遗留问题和下一阶段规划

**整体评估：生产级完备性 92%**（与第三期持平，进入稳定期）

---

## 一、项目演进总览

### 1.1 四期演进脉络

```
第一期（基准审计 85%）
    │
    │  识别问题：Director 硬编码、Skill 未集成、工具选择僵化
    │
    ▼
第二期（调研规划）
    │
    │  提出 5 Phase 实现计划：LLM Client → Tool Schema → Prompt → Director 集成 → 整合
    │
    ▼
第三期（架构重构 92%）
    │
    │  第二期计划 100% 完成
    │  新架构：SkillBasedDirector + Pipeline V2 + Skill 模块系统
    │
    ▼
第四期（文档完善 92%）
    │
    │  项目文档全面更新
    │  开发规范固化
    │  为后续迭代奠定一致性基础
```

### 1.2 核心指标对比

| 指标 | 第一期 | 第三期 | 第四期 |
|------|--------|--------|--------|
| 生产级完备性 | 85% | 92% | 92% |
| 测试数量 | 230 | 378 | 378 |
| Agent 协作完备性 | 80% | 95% | 95% |
| Web 应用完备性 | 85% | 95% | 95% |
| 文档完备性 | 70% | 80% | 95% |
| Skill 模块数 | 0 | 7 | 7 |
| Web 路由数 | 10 | 11 | 11 |
| LLM 集成模式 | 规则引擎 | 规则+LLM | 规则+LLM |

---

## 二、当前架构总览

### 2.1 Agent 架构

| Agent | 文件 | 行数 | 职责 |
|-------|------|------|------|
| DirectorAgent | `director.py` | 1906 | 主控导演、上下文压缩、路由、风格感知指令、Skill 集成 |
| SkillBasedDirector | `director_v2.py` | 1142 | 基于 Skill 的主控 Agent（新架构） |
| LibrarianAgent | `librarian.py` | 770 | 上下文感知节拍生成、结构化草稿、智能重写 |
| LoreCheckerAgent | `lore_checker.py` | 461 | 逻辑审查（关键词/场景/人物）+ 跨章节一致性 |
| StylistAgent | `stylist.py` | 548 | AI 痕迹检测、节奏验证、声音一致性、分类评分 |
| ReaderAgent | `reader.py` | 738 | 批量阅读原著、三层抽取（craft/style/novel） |
| StyleDirectorAgent | `style_director.py` | 781 | 分层差异分析、偏差检测、收敛追踪 |
| SimulatorAgent | `simulator.py` | 533 | 全流程编排、风格系统对接、跨章节预检 |
| InitializerAgent | `initializer.py` | 337 | 项目初始化 |
| PipelineV2 | `pipeline_v2.py` | 516 | 带人工审核的完整流程 |

**总代码量**：~8000 行（agents 目录）

### 2.2 LLM 集成层

| 模块 | 文件 | 行数 | 职责 |
|------|------|------|------|
| LLMClient | `client.py` | 331 | LiteLLM 封装、JSON Mode、多模型支持 |
| ModelRouter | `router.py` | 100 | 任务类型路由（reasoning/generation/review/style） |
| LLMConfig | `config.py` | 109 | 配置加载、模型池管理 |
| ToolSchema | `tool_schema.py` | 495 | OpenAI-compatible schema 生成 |
| Prompts | `prompts.py` | 475 | Prompt 模板管理 |

**总代码量**：~1500 行（llm 目录）

### 2.3 Skill 模块系统

```
skills/
├── __init__.py           # 模块入口
├── skill.py              # Skill 数据模型（220行）
├── skill_registry.py     # 功能注册表（132行）
├── skill_loader.py       # 文件系统加载器（132行）
├── tools/
│   └── executor.py       # 统一工具执行器（13个工具）
├── outline/              # 大纲功能模块
│   ├── SKILL.md          # 功能指令文档
│   ├── tools/            # parser, serializer, validator
│   └── prompts/          # 生成提示词模板
├── writing/              # 写作功能模块
│   ├── SKILL.md
│   ├── tools/            # beat_generator, draft_generator
│   ├── workflows/        # chapter_writing.yaml
│   └── prompts/          # 节拍/段落模板
├── style/                # 风格功能模块
├── character/            # 角色功能模块
├── foreshadowing/        # 伏笔功能模块
├── world/                # 世界观功能模块
└── project/              # 项目管理功能模块
```

**7 个功能模块**：outline, writing, style, character, foreshadowing, world, project

---

## 三、Web 应用状态

### 3.1 页面路由（11 模板）

| 路由 | 模板 | 行数 | 功能 |
|------|------|------|------|
| `/` | dashboard.html | 68 | 仪表盘 |
| `/chat` | chat.html | 795 | 统一对话界面（核心功能） |
| `/editor` | editor.html | 800 | 章节编辑器 + AI 助手 |
| `/settings` | settings.html | 300 | LLM 配置 |
| `/characters` | characters.html | 180 | 人物管理 |
| `/foreshadowing` | foreshadowing.html | 190 | 伏笔 DAG |
| `/world` | world.html | 117 | 世界观图谱 |
| `/style` | style.html | 82 | 风格分析 |
| `/timeline` | timeline.html | 77 | 叙事时间线 |
| `/novels/new` | novel_new.html | 186 | 新建项目 |
| `base.html` | base.html | 160 | 基础模板 |

**总模板代码**：~3000 行

### 3.2 API 端点（67+）

- **Workflow API** (5): 工作流列表、详情、启动、对话、注册表摘要
- **Pipeline V2 API** (4): 启动、状态、SSE 流、批准
- **Outline API** (15): 完整的 CRUD + 导入导出
- **Character API** (10): CRUD + 时间线 + 快照
- **World API** (8): 实体、关系、冲突检查
- **Foreshadowing API** (8): DAG 管理、状态统计
- **Style API** (6): 合成、分析、档案
- **Settings API** (5): LLM 配置管理
- **其他**: Analysis、Narrative 等

---

## 四、三层风格架构

### 4.1 架构说明

```
用户偏好覆盖 > 作品设定（硬性约束） > 作品风格（核心约束） > 通用技法（可选参考）
```

| 层级 | 目录 | 文件数 | 约束力 | 说明 |
|------|------|--------|--------|------|
| **通用技法** | `craft/` | 8 | 可选参考 | 场景结构、信息揭示策略、张力模型 |
| **作品风格** | `styles/{作品}/` | 8+ | 应当遵循 | 叙述声音、幽默体系、语言偏好、节奏 |
| **作品设定** | `novels/{作品}/` | 6 | 不可违反 | 角色一致性、术语、世界观规则 |

### 4.2 实际内容

**craft/ 通用技法（8 文件）**：
- `dialogue_craft.md` — 对话格式规范、节奏光谱
- `information_reveal.md` — 信息揭示策略
- `language_craft.md` — 语言风格技法
- `pov_techniques.md` — 视角技巧
- `rhythm_craft.md` — 节奏控制
- `scene_structures.md` — 场景结构模式
- `tension_patterns.md` — 张力模型
- `humanization.yaml` — 人性化处理配置

**styles/术师手册/ 作品风格（15+ 文件）**：
- `fingerprint.md` — 风格 DNA（12 个核心标识）
- `voice.md` — 叙述者声音
- `language.md` — 语言风格
- `rhythm.md` — 节奏风格
- `humor.md` — 幽默体系
- `dialogue_craft.md` — 作品级对话风格
- `iteration_log.md` — 风格迭代记录
- `reader_notes/` — Reader Agent 提取的完整笔记（7 个子文件）

**novels/术师手册/ 作品设定（6 文件）**：
- `characters.md` — 角色设定（亚修·希斯、索妮娅等完整档案）
- `worldbuilding_rules.md` — 世界观规则（硬性约束）
- `terminology.md` — 术语表
- `scene_instances.md` — 名场面实例
- `initial.md` — 大纲
- `notes.md` — 大纲注意事项

---

## 五、完整目录结构

```
OpenWrite/
├── AGENTS.md                    # AI Agent 开发指南（本文档）
├── README.md                    # 项目说明文档
├── requirements.txt             # Python 依赖
├── llm_config.yaml              # LLM 配置（自动生成，gitignored）
│
├── tools/                       # Python 源码
│   ├── __init__.py
│   ├── cli.py                   # CLI 入口（Typer app）
│   │
│   ├── agents/                  # Agent 实现（~8000 行）
│   │   ├── __init__.py
│   │   ├── director.py          # DirectorAgent（1906 行）
│   │   ├── director_v2.py       # SkillBasedDirector（1142 行）
│   │   ├── librarian.py         # LibrarianAgent（770 行）
│   │   ├── lore_checker.py      # LoreCheckerAgent（461 行）
│   │   ├── stylist.py           # StylistAgent（548 行）
│   │   ├── reader.py            # ReaderAgent（738 行）
│   │   ├── style_director.py    # StyleDirectorAgent（781 行）
│   │   ├── simulator.py         # SimulatorAgent（533 行）
│   │   ├── initializer.py       # InitializerAgent（337 行）
│   │   └── pipeline_v2.py       # PipelineV2（516 行）
│   │
│   ├── models/                  # Pydantic 数据模型
│   │   ├── __init__.py
│   │   ├── outline.py           # OutlineHierarchy, OutlineNode
│   │   ├── character.py         # CharacterCard, CharacterProfile
│   │   ├── style.py             # StyleProfile, VoicePattern
│   │   ├── context_package.py   # GenerationContext
│   │   ├── workflow.py          # WorkflowDefinition, WorkflowPhase
│   │   └── intent.py            # IntentDecision, DirectorResponse
│   │
│   ├── llm/                     # LLM 集成层（~1500 行）
│   │   ├── __init__.py
│   │   ├── client.py            # LLMClient（331 行）
│   │   ├── config.py            # LLMConfig（109 行）
│   │   ├── router.py            # ModelRouter（100 行）
│   │   ├── tool_schema.py       # ToolSchema（495 行）
│   │   └── prompts.py           # Prompts（475 行）
│   │
│   ├── utils/                   # 工具函数
│   │   ├── style_composer.py    # StyleComposer
│   │   ├── compressor.py        # ProgressiveCompressor
│   │   └── outline_serializer.py # OutlineMdSerializer
│   │
│   ├── parsers/                 # Markdown 解析器
│   │   └── outline_md_parser.py # OutlineMdParser
│   │
│   ├── graph/                   # 图结构
│   │   ├── foreshadowing_dag.py # ForeshadowingDAG
│   │   └── world_graph.py       # WorldGraph
│   │
│   ├── checks/                  # 逻辑检查
│   │   └── lore_checks.py       # LoreChecker 规则
│   │
│   ├── queries/                 # 查询接口
│   │   └── queries.py           # 数据查询 API
│   │
│   └── web/                     # Web 应用
│       ├── __init__.py          # FastAPI app + 路由
│       ├── templates/           # Jinja2 模板（11 个）
│       │   ├── base.html
│       │   ├── dashboard.html
│       │   ├── chat.html        # 统一对话界面
│       │   ├── editor.html
│       │   ├── settings.html
│       │   ├── characters.html
│       │   ├── foreshadowing.html
│       │   ├── world.html
│       │   ├── style.html
│       │   ├── timeline.html
│       │   └── novel_new.html
│       └── static/              # CSS/JS 静态资源
│
├── skills/                      # Skill 模块系统
│   ├── __init__.py
│   ├── skill.py                 # Skill 数据模型
│   ├── skill_registry.py        # 功能注册表
│   ├── skill_loader.py          # 文件系统加载器
│   ├── tools/
│   │   └── executor.py          # 统一工具执行器
│   ├── outline/                 # 大纲功能模块
│   ├── writing/                 # 写作功能模块
│   ├── style/                   # 风格功能模块
│   ├── character/               # 角色功能模块
│   ├── foreshadowing/           # 伏笔功能模块
│   ├── world/                   # 世界观功能模块
│   └── project/                 # 项目管理功能模块
│
├── .agents/                     # Agent Skills（旧版）
│   └── skills/
│       ├── openwrite-overview/
│       └── style-system/
│
├── craft/                       # 通用写作技法（跨作品）
│   ├── dialogue_craft.md
│   ├── information_reveal.md
│   ├── language_craft.md
│   ├── pov_techniques.md
│   ├── rhythm_craft.md
│   ├── scene_structures.md
│   ├── tension_patterns.md
│   └── humanization.yaml
│
├── styles/                      # 作者/作品风格指纹
│   └── 术师手册/
│       ├── fingerprint.md
│       ├── voice.md
│       ├── language.md
│       ├── rhythm.md
│       ├── humor.md
│       ├── dialogue_craft.md
│       ├── iteration_log.md
│       └── reader_notes/
│           ├── consistency.md
│           ├── dialogue.md
│           ├── language.md
│           ├── narrative.md
│           ├── rhythm.md
│           ├── scene_templates.md
│           └── summary.md
│
├── novels/                      # 作品设定（硬性约束）
│   └── 术师手册/
│       ├── characters.md
│       ├── worldbuilding_rules.md
│       ├── terminology.md
│       ├── scene_instances.md
│       ├── initial.md
│       └── notes.md
│
├── data/                        # 运行时数据
│   ├── novels/{novel_id}/
│   │   ├── outline/
│   │   ├── characters/
│   │   ├── world/
│   │   ├── foreshadowing/
│   │   ├── manuscript/
│   │   └── narrative/
│   ├── reference_books/         # 参考原著分块（6 部作品）
│   └── sessions/                # 会话数据
│
├── docs/                        # 文档
│   ├── CURRENT_ARCHITECTURE.md
│   ├── ARCHITECTURE_REFACTOR.md
│   ├── INTENT_ROUTING_PLAN.md
│   ├── WORKFLOW_DIRECTOR_PLAN.md
│   ├── OUTLINE_MD_SPEC.md
│   ├── prompts/
│   │   ├── PROMPT_READER.md
│   │   ├── PROMPT_DIRECTOR.md
│   │   ├── COMPOSE_RULES.md
│   │   └── task.md
│   ├── workflows/
│   │   ├── style_workflow.md
│   │   └── outline_workflow.md
│   └── archive/                 # 历史进度报告
│
├── tests/                       # pytest 测试（16 文件，378 测试）
│   ├── test_phase7.py           # Phase 7 功能（59 测试）
│   ├── test_workflow.py         # 工作流集成（38 测试）
│   ├── test_director_v2.py      # Director V2（35 测试）
│   ├── test_world_advanced.py   # 世界图谱高级（43 测试）
│   ├── test_narrative.py        # 叙事生成（27 测试）
│   ├── test_llm.py              # LLM 集成
│   ├── test_web.py              # Web 应用
│   └── ...                      # 其他测试
│
├── composed/                    # 风格合成输出
├── config/                      # 配置文件
│   └── main_prompt.md           # 主 AI 提示词
│
└── 报告文件
    ├── 第一期报告.md
    ├── 第二期报告.md
    ├── 第三期报告.md
    └── 第四期报告.md            # 本文件
```

---

## 六、编码规范（固化）

### 6.1 类型注解

- 所有函数签名**必须**有 type hints
- 使用 `Optional[T]` 而非 `T | None`（兼容 Python 3.9）
- 使用 `list[T]` 而非 `List[T]`（Python 3.9+）

### 6.2 数据模型

- 使用 Pydantic `BaseModel`，不用 `dataclass`
- 字段必须有类型注解和默认值或 `Field()`
- 使用 `Field(default_factory=list)` 处理可变默认值

### 6.3 错误处理

- **禁止**空 catch 块：`except Exception: pass`
- **禁止**类型抑制：`# type: ignore`
- **禁止**任意类型：`as any`
- 使用具体异常类型，提供有意义的错误消息

### 6.4 导入顺序

```python
# 1. 标准库
from pathlib import Path
from typing import Dict, List, Optional

# 2. 第三方库
import yaml
from pydantic import BaseModel, Field

# 3. 本地模块
from tools.models.outline import OutlineHierarchy
```

### 6.5 文档字符串

- 中文，Google style
- 模块级文档在文件开头
- 类和公共方法必须有 docstring

```python
def generate_chapter(self, chapter_id: str, context: Dict[str, str]) -> str:
    """生成章节草稿。
    
    Args:
        chapter_id: 章节标识符
        context: 上下文信息字典
    
    Returns:
        生成的章节文本
    """
```

### 6.6 命名约定

- 文件名：snake_case（`outline_md_parser.py`）
- 类名：PascalCase（`OutlineHierarchy`）
- 函数/方法：snake_case（`parse_outline_md`）
- 常量：UPPER_SNAKE_CASE（`RECENT_TEXT_MAX = 1000`）
- 私有方法：_leading_underscore（`_extract_yaml_block`）

---

## 七、开发命令速查

### 7.1 构建与测试

```bash
# 安装依赖
pip install -r requirements.txt

# 运行全部测试（378 tests expected）
python3 -m pytest -q

# 运行单个测试文件
python3 -m pytest tests/test_workflow.py -v

# 运行单个测试函数
python3 -m pytest tests/test_llm.py::test_router_get_routes -v

# 运行带覆盖率报告
python3 -m pytest --cov=tools --cov-report=term-missing
```

### 7.2 CLI 命令

```bash
# CLI 入口
python3 -m tools.cli --help

# 初始化项目
python3 -m tools.cli init my_novel

# 创建人物
python3 -m tools.cli character create 李逍遥 --tier 主角

# 模拟章节生成
python3 -m tools.cli simulate chapter --id ch_003 --novel-id my_novel

# 风格系统
python3 -m tools.cli style compose --novel-id 术师手册 --style-id 术师手册
python3 -m tools.cli style read-batch --file path/to/text.txt --novel-id 术师手册
```

### 7.3 Web 应用

```bash
# 启动 Web 服务
python3 -m tools.web
# 访问 http://localhost:8000
```

---

## 八、LLM 配置

### 8.1 多模型池配置（`llm_config.yaml`）

```yaml
enabled: true
models:
  Claude-Opus-4.6:
    model: claude-opus-4-6
    api_base: https://aws.d68.fun/v1/messages
    api_key_env: CLAUDE_API_KEY
    max_tokens: 80000
  Kimi-K2.5:
    model: kimi-k2.5
    api_base: https://api.moonshot.cn/v1/chat/completions
    api_key_env: KIMI_API_KEY
    max_tokens: 16000

routes:
  reasoning:    # Director 决策
    models: [Claude-Opus-4.6, GLM-4.7, DeepSeek]
    primary_index: 0
  generation:   # Writer 生成
    models: [Kimi-K2.5, MiniMax-M2.5, ChatGPT-5.3]
    primary_index: 0
```

### 8.2 环境变量设置

```bash
export KIMI_API_KEY=sk-xxx
export CLAUDE_API_KEY=sk-xxx
export GLM_API_KEY=sk-xxx
export DEEPSEEK_API_KEY=sk-xxx
export MINIMAX_API_KEY=sk-xxx
```

---

## 九、遗留问题与下一阶段规划

### 9.1 已解决（第一期 → 第四期）

| 问题 | 第一期状态 | 第四期状态 |
|------|------------|------------|
| Director 工具选择硬编码 | ❌ 硬编码 | ✅ SkillBasedDirector 动态发现 |
| Skill 系统未集成 | ❌ 未使用 | ✅ 完整集成（7 模块） |
| 无文件读取能力 | ❌ 无 | ✅ ToolExecutor 13 个工具 |
| LoreChecker 未集成 WorldRuleEngine | ⚠️ 未集成 | ✅ Pipeline V2 已集成 |
| Stylist 无情境分类器 | ❌ 未实现 | ⚠️ 部分实现（通过 Skill 模块） |
| 无人工审核环节 | ❌ 无 | ✅ Pipeline V2 |

### 9.2 待改进项

| 优先级 | 问题 | 建议 |
|--------|------|------|
| 🔴 高 | `/novels/new` 路由缺失 | 添加 `@app.get("/novels/new")` 路由 |
| 🟡 中 | 旧 `simulator.py` 待废弃 | 制定迁移计划到 Pipeline V2 |
| 🟡 中 | LLM 无流式响应支持 | 添加 `stream=True` 支持 |
| 🟡 中 | 世界图谱可视化未完成 | 继续 Phase 6C 规划 |
| 🟢 低 | Token 使用统计缺失 | 添加会话级 token 追踪 |
| 🟢 低 | 角色状态快照/回滚 | Phase 9 规划 |

### 9.3 Phase 8-9 规划

**Phase 8: 稳定化与优化**
1. 修复 Web 路由（`/novels/new`）
2. 性能优化（Skill 缓存、Tool Schema 按需加载）
3. LLM 流式响应支持
4. `simulator.py` → `pipeline_v2.py` 迁移

**Phase 9: 高级功能**
1. 世界图谱可视化（Phase 6C 继续）
2. 角色状态快照/回滚
3. 多语言支持
4. 插件系统设计

---

## 十、结论

**第四期核心成就**：

1. ✅ **文档完备性提升** — AGENTS.md 和 README.md 全面更新至最新架构
2. ✅ **开发规范固化** — 目录结构、编码规范、命令速查完整记录
3. ✅ **一致性保障** — 为后续对话和新开发者提供完整上下文

**项目状态总结**：

| 维度 | 状态 | 说明 |
|------|------|------|
| 核心架构 | ✅ 稳定 | Skill 系统 + Pipeline V2 完整 |
| Agent 实现 | ✅ 完整 | 10 个 Agent，~8000 行代码 |
| LLM 集成 | ✅ 可用 | 多模型池 + 任务路由 |
| Web 应用 | ✅ 可用 | 11 模板，67+ API |
| 测试覆盖 | ✅ 充分 | 378 测试，无跳过 |
| 文档 | ✅ 完善 | AGENTS.md + README.md + 本报告 |

**生产级完备性评估：92%**

项目已进入稳定迭代阶段，核心功能完备，文档齐全，可支持后续功能扩展和优化。

---

*报告生成时间: 2026-03-02*
